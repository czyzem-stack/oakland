using UnityEngine;

public class HeroSettings : MonoBehaviour
{
    [Tooltip("If enabled, Steve will be protected from death by a Second Wind (FTUE Plot Armor).")]
    public bool secondWind = false;
}
